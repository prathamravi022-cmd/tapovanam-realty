import { c as createLucideIcon, i as isEasingArray, a as interpolate, f as frame, b as isMotionValue, J as JSAnimation, V as VisualElement, d as createBox, u as useConstant, m as motionValue, r as reactExports, M as MotionConfigContext, e as useIsomorphicLayoutEffect, g as cancelFrame, h as collectMotionValues, j as resolveElements, k as mixNumber, l as removeItem, n as defaultOffset, o as createGeneratorEasing, p as fillOffset, q as isGenerator, s as secondsToMilliseconds, t as progress, v as isSVGElement, w as isSVGSVGElement, S as SVGVisualElement, H as HTMLVisualElement, x as visualElementStore, y as animateSingleValue, z as animateTarget, A as spring, B as jsxRuntimeExports, C as motion, L as Link, D as cn, P as PropertyStatus, E as AnimatePresence, F as useQueryClient, G as useDealerProfile, I as Input, X, K as Button, N as Label } from "./index-D6obxqBN.js";
import { B as Badge } from "./badge-DMkhsvL2.js";
import { S as Skeleton } from "./skeleton-24zo03YF.js";
import { g as getPrimaryImageUrl, P as PropertyStatusBadge, f as formatPrice, a as formatArea } from "./StatusBadge-cIrDUTo3.js";
import { C as ChevronLeft, M as MapPin } from "./map-pin-BZS1lrIG.js";
import { C as ChevronRight, H as Heart, R as Ruler, M as MessageCircle, a as Clock } from "./ruler-BU45pwlt.js";
import { P as PROPERTIES_QUERY_KEY, u as useProperties } from "./useProperties-SsI244bB.js";
import { C as ChevronUp, a as ChevronDown } from "./chevron-up-mXdalfp2.js";
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$5 = [
  ["path", { d: "m3 16 4 4 4-4", key: "1co6wj" }],
  ["path", { d: "M7 20V4", key: "1yoxec" }],
  ["path", { d: "m21 8-4-4-4 4", key: "1c9v7m" }],
  ["path", { d: "M17 4v16", key: "7dpous" }]
];
const ArrowDownUp = createLucideIcon("arrow-down-up", __iconNode$5);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$4 = [
  ["path", { d: "M15 21v-8a1 1 0 0 0-1-1h-4a1 1 0 0 0-1 1v8", key: "5wwlr5" }],
  [
    "path",
    {
      d: "M3 10a2 2 0 0 1 .709-1.528l7-5.999a2 2 0 0 1 2.582 0l7 5.999A2 2 0 0 1 21 10v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z",
      key: "1d0kgt"
    }
  ]
];
const House = createLucideIcon("house", __iconNode$4);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$3 = [
  ["path", { d: "M3 12a9 9 0 0 1 9-9 9.75 9.75 0 0 1 6.74 2.74L21 8", key: "v9h5vc" }],
  ["path", { d: "M21 3v5h-5", key: "1q7to0" }],
  ["path", { d: "M21 12a9 9 0 0 1-9 9 9.75 9.75 0 0 1-6.74-2.74L3 16", key: "3uifl3" }],
  ["path", { d: "M8 16H3v5", key: "1cv678" }]
];
const RefreshCw = createLucideIcon("refresh-cw", __iconNode$3);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$2 = [
  ["path", { d: "m21 21-4.34-4.34", key: "14j7rj" }],
  ["circle", { cx: "11", cy: "11", r: "8", key: "4ej97u" }]
];
const Search = createLucideIcon("search", __iconNode$2);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$1 = [
  ["line", { x1: "21", x2: "14", y1: "4", y2: "4", key: "obuewd" }],
  ["line", { x1: "10", x2: "3", y1: "4", y2: "4", key: "1q6298" }],
  ["line", { x1: "21", x2: "12", y1: "12", y2: "12", key: "1iu8h1" }],
  ["line", { x1: "8", x2: "3", y1: "12", y2: "12", key: "ntss68" }],
  ["line", { x1: "21", x2: "16", y1: "20", y2: "20", key: "14d8ph" }],
  ["line", { x1: "12", x2: "3", y1: "20", y2: "20", key: "m0wm8r" }],
  ["line", { x1: "14", x2: "14", y1: "2", y2: "6", key: "14e1ph" }],
  ["line", { x1: "8", x2: "8", y1: "10", y2: "14", key: "1i6ji0" }],
  ["line", { x1: "16", x2: "16", y1: "18", y2: "22", key: "1lctlv" }]
];
const SlidersHorizontal = createLucideIcon("sliders-horizontal", __iconNode$1);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode = [
  [
    "path",
    {
      d: "M9.937 15.5A2 2 0 0 0 8.5 14.063l-6.135-1.582a.5.5 0 0 1 0-.962L8.5 9.936A2 2 0 0 0 9.937 8.5l1.582-6.135a.5.5 0 0 1 .963 0L14.063 8.5A2 2 0 0 0 15.5 9.937l6.135 1.581a.5.5 0 0 1 0 .964L15.5 14.063a2 2 0 0 0-1.437 1.437l-1.582 6.135a.5.5 0 0 1-.963 0z",
      key: "4pj2yx"
    }
  ],
  ["path", { d: "M20 3v4", key: "1olli1" }],
  ["path", { d: "M22 5h-4", key: "1gvqau" }],
  ["path", { d: "M4 17v2", key: "vumght" }],
  ["path", { d: "M5 18H3", key: "zchphs" }]
];
const Sparkles = createLucideIcon("sparkles", __iconNode);
const wrap = (min, max, v) => {
  const rangeSize = max - min;
  return ((v - min) % rangeSize + rangeSize) % rangeSize + min;
};
function getEasingForSegment(easing, i) {
  return isEasingArray(easing) ? easing[wrap(0, easing.length, i)] : easing;
}
class GroupAnimation {
  constructor(animations) {
    this.stop = () => this.runAll("stop");
    this.animations = animations.filter(Boolean);
  }
  get finished() {
    return Promise.all(this.animations.map((animation) => animation.finished));
  }
  /**
   * TODO: Filter out cancelled or stopped animations before returning
   */
  getAll(propName) {
    return this.animations[0][propName];
  }
  setAll(propName, newValue) {
    for (let i = 0; i < this.animations.length; i++) {
      this.animations[i][propName] = newValue;
    }
  }
  attachTimeline(timeline) {
    const subscriptions = this.animations.map((animation) => animation.attachTimeline(timeline));
    return () => {
      subscriptions.forEach((cancel, i) => {
        cancel && cancel();
        this.animations[i].stop();
      });
    };
  }
  get time() {
    return this.getAll("time");
  }
  set time(time) {
    this.setAll("time", time);
  }
  get speed() {
    return this.getAll("speed");
  }
  set speed(speed) {
    this.setAll("speed", speed);
  }
  get state() {
    return this.getAll("state");
  }
  get startTime() {
    return this.getAll("startTime");
  }
  get duration() {
    return getMax(this.animations, "duration");
  }
  get iterationDuration() {
    return getMax(this.animations, "iterationDuration");
  }
  runAll(methodName) {
    this.animations.forEach((controls) => controls[methodName]());
  }
  play() {
    this.runAll("play");
  }
  pause() {
    this.runAll("pause");
  }
  cancel() {
    this.runAll("cancel");
  }
  complete() {
    this.runAll("complete");
  }
}
function getMax(animations, propName) {
  let max = 0;
  for (let i = 0; i < animations.length; i++) {
    const value = animations[i][propName];
    if (value !== null && value > max) {
      max = value;
    }
  }
  return max;
}
class GroupAnimationWithThen extends GroupAnimation {
  then(onResolve, _onReject) {
    return this.finished.finally(onResolve).then(() => {
    });
  }
}
function transform(...args) {
  const useImmediate = !Array.isArray(args[0]);
  const argOffset = useImmediate ? 0 : -1;
  const inputValue = args[0 + argOffset];
  const inputRange = args[1 + argOffset];
  const outputRange = args[2 + argOffset];
  const options = args[3 + argOffset];
  const interpolator = interpolate(inputRange, outputRange, options);
  return useImmediate ? interpolator(inputValue) : interpolator;
}
function attachFollow(value, source, options = {}) {
  const initialValue = value.get();
  let activeAnimation = null;
  let latestValue = initialValue;
  let latestSetter;
  const unit = typeof initialValue === "string" ? initialValue.replace(/[\d.-]/g, "") : void 0;
  const stopAnimation = () => {
    if (activeAnimation) {
      activeAnimation.stop();
      activeAnimation = null;
    }
    value.animation = void 0;
  };
  const startAnimation = () => {
    const currentValue = asNumber(value.get());
    const targetValue = asNumber(latestValue);
    if (currentValue === targetValue) {
      stopAnimation();
      return;
    }
    const velocity = activeAnimation ? activeAnimation.getGeneratorVelocity() : value.getVelocity();
    stopAnimation();
    activeAnimation = new JSAnimation({
      keyframes: [currentValue, targetValue],
      velocity,
      // Default to spring if no type specified (matches useSpring behavior)
      type: "spring",
      restDelta: 1e-3,
      restSpeed: 0.01,
      ...options,
      onUpdate: latestSetter
    });
  };
  const scheduleAnimation = () => {
    var _a;
    startAnimation();
    value.animation = activeAnimation ?? void 0;
    (_a = value["events"].animationStart) == null ? void 0 : _a.notify();
    activeAnimation == null ? void 0 : activeAnimation.then(() => {
      var _a2;
      value.animation = void 0;
      (_a2 = value["events"].animationComplete) == null ? void 0 : _a2.notify();
    });
  };
  value.attach((v, set) => {
    latestValue = v;
    latestSetter = (latest) => set(parseValue(latest, unit));
    frame.postRender(scheduleAnimation);
  }, stopAnimation);
  if (isMotionValue(source)) {
    let skipNextAnimation = options.skipInitialAnimation === true;
    const removeSourceOnChange = source.on("change", (v) => {
      if (skipNextAnimation) {
        skipNextAnimation = false;
        value.jump(parseValue(v, unit), false);
      } else {
        value.set(parseValue(v, unit));
      }
    });
    const removeValueOnDestroy = value.on("destroy", removeSourceOnChange);
    return () => {
      removeSourceOnChange();
      removeValueOnDestroy();
    };
  }
  return stopAnimation;
}
function parseValue(v, unit) {
  return unit ? v + unit : v;
}
function asNumber(v) {
  return typeof v === "number" ? v : parseFloat(v);
}
function isObjectKey(key, object) {
  return key in object;
}
class ObjectVisualElement extends VisualElement {
  constructor() {
    super(...arguments);
    this.type = "object";
  }
  readValueFromInstance(instance, key) {
    if (isObjectKey(key, instance)) {
      const value = instance[key];
      if (typeof value === "string" || typeof value === "number") {
        return value;
      }
    }
    return void 0;
  }
  getBaseTargetFromProps() {
    return void 0;
  }
  removeValueFromRenderState(key, renderState) {
    delete renderState.output[key];
  }
  measureInstanceViewportBox() {
    return createBox();
  }
  build(renderState, latestValues) {
    Object.assign(renderState.output, latestValues);
  }
  renderInstance(instance, { output }) {
    Object.assign(instance, output);
  }
  sortInstanceNodePosition() {
    return 0;
  }
}
function useMotionValue(initial) {
  const value = useConstant(() => motionValue(initial));
  const { isStatic } = reactExports.useContext(MotionConfigContext);
  if (isStatic) {
    const [, setLatest] = reactExports.useState(initial);
    reactExports.useEffect(() => value.on("change", setLatest), []);
  }
  return value;
}
function useCombineMotionValues(values, combineValues) {
  const value = useMotionValue(combineValues());
  const updateValue = () => value.set(combineValues());
  updateValue();
  useIsomorphicLayoutEffect(() => {
    const scheduleUpdate = () => frame.preRender(updateValue, false, true);
    const subscriptions = values.map((v) => v.on("change", scheduleUpdate));
    return () => {
      subscriptions.forEach((unsubscribe) => unsubscribe());
      cancelFrame(updateValue);
    };
  });
  return value;
}
function useComputed(compute) {
  collectMotionValues.current = [];
  compute();
  const value = useCombineMotionValues(collectMotionValues.current, compute);
  collectMotionValues.current = void 0;
  return value;
}
function useTransform(input, inputRangeOrTransformer, outputRangeOrMap, options) {
  if (typeof input === "function") {
    return useComputed(input);
  }
  const isOutputMap = outputRangeOrMap !== void 0 && !Array.isArray(outputRangeOrMap) && typeof inputRangeOrTransformer !== "function";
  if (isOutputMap) {
    return useMapTransform(input, inputRangeOrTransformer, outputRangeOrMap, options);
  }
  const outputRange = outputRangeOrMap;
  const transformer = typeof inputRangeOrTransformer === "function" ? inputRangeOrTransformer : transform(inputRangeOrTransformer, outputRange, options);
  const result = Array.isArray(input) ? useListTransform(input, transformer) : useListTransform([input], ([latest]) => transformer(latest));
  const inputAccelerate = !Array.isArray(input) ? input.accelerate : void 0;
  if (inputAccelerate && !inputAccelerate.isTransformed && typeof inputRangeOrTransformer !== "function" && Array.isArray(outputRangeOrMap) && (options == null ? void 0 : options.clamp) !== false) {
    result.accelerate = {
      ...inputAccelerate,
      times: inputRangeOrTransformer,
      keyframes: outputRangeOrMap,
      isTransformed: true,
      ...{}
    };
  }
  return result;
}
function useListTransform(values, transformer) {
  const latest = useConstant(() => []);
  return useCombineMotionValues(values, () => {
    latest.length = 0;
    const numValues = values.length;
    for (let i = 0; i < numValues; i++) {
      latest[i] = values[i].get();
    }
    return transformer(latest);
  });
}
function useMapTransform(inputValue, inputRange, outputMap, options) {
  const keys = useConstant(() => Object.keys(outputMap));
  const output = useConstant(() => ({}));
  for (const key of keys) {
    output[key] = useTransform(inputValue, inputRange, outputMap[key], options);
  }
  return output;
}
function useFollowValue(source, options = {}) {
  const { isStatic } = reactExports.useContext(MotionConfigContext);
  const getFromSource = () => isMotionValue(source) ? source.get() : source;
  if (isStatic) {
    return useTransform(getFromSource);
  }
  const value = useMotionValue(getFromSource());
  reactExports.useInsertionEffect(() => {
    return attachFollow(value, source, options);
  }, [value, JSON.stringify(options)]);
  return value;
}
function useSpring(source, options = {}) {
  return useFollowValue(source, { type: "spring", ...options });
}
function isDOMKeyframes(keyframes) {
  return typeof keyframes === "object" && !Array.isArray(keyframes);
}
function resolveSubjects(subject, keyframes, scope, selectorCache) {
  if (subject == null) {
    return [];
  }
  if (typeof subject === "string" && isDOMKeyframes(keyframes)) {
    return resolveElements(subject, scope, selectorCache);
  } else if (subject instanceof NodeList) {
    return Array.from(subject);
  } else if (Array.isArray(subject)) {
    return subject.filter((s) => s != null);
  } else {
    return [subject];
  }
}
function calculateRepeatDuration(duration, repeat, _repeatDelay) {
  return duration * (repeat + 1);
}
function calcNextTime(current, next, prev, labels) {
  if (typeof next === "number") {
    return next;
  } else if (next.startsWith("-") || next.startsWith("+")) {
    return Math.max(0, current + parseFloat(next));
  } else if (next === "<") {
    return prev;
  } else if (next.startsWith("<")) {
    return Math.max(0, prev + parseFloat(next.slice(1)));
  } else {
    return labels.get(next) ?? current;
  }
}
function eraseKeyframes(sequence, startTime, endTime) {
  for (let i = 0; i < sequence.length; i++) {
    const keyframe = sequence[i];
    if (keyframe.at > startTime && keyframe.at < endTime) {
      removeItem(sequence, keyframe);
      i--;
    }
  }
}
function addKeyframes(sequence, keyframes, easing, offset, startTime, endTime) {
  eraseKeyframes(sequence, startTime, endTime);
  for (let i = 0; i < keyframes.length; i++) {
    sequence.push({
      value: keyframes[i],
      at: mixNumber(startTime, endTime, offset[i]),
      easing: getEasingForSegment(easing, i)
    });
  }
}
function normalizeTimes(times, repeat) {
  for (let i = 0; i < times.length; i++) {
    times[i] = times[i] / (repeat + 1);
  }
}
function compareByTime(a, b) {
  if (a.at === b.at) {
    if (a.value === null)
      return 1;
    if (b.value === null)
      return -1;
    return 0;
  } else {
    return a.at - b.at;
  }
}
const defaultSegmentEasing = "easeInOut";
function createAnimationsFromSequence(sequence, { defaultTransition = {}, ...sequenceTransition } = {}, scope, generators) {
  const defaultDuration = defaultTransition.duration || 0.3;
  const animationDefinitions = /* @__PURE__ */ new Map();
  const sequences = /* @__PURE__ */ new Map();
  const elementCache = {};
  const timeLabels = /* @__PURE__ */ new Map();
  let prevTime = 0;
  let currentTime = 0;
  let totalDuration = 0;
  for (let i = 0; i < sequence.length; i++) {
    const segment = sequence[i];
    if (typeof segment === "string") {
      timeLabels.set(segment, currentTime);
      continue;
    } else if (!Array.isArray(segment)) {
      timeLabels.set(segment.name, calcNextTime(currentTime, segment.at, prevTime, timeLabels));
      continue;
    }
    let [subject, keyframes, transition = {}] = segment;
    if (transition.at !== void 0) {
      currentTime = calcNextTime(currentTime, transition.at, prevTime, timeLabels);
    }
    let maxDuration = 0;
    const resolveValueSequence = (valueKeyframes, valueTransition, valueSequence, elementIndex = 0, numSubjects = 0) => {
      const valueKeyframesAsList = keyframesAsList(valueKeyframes);
      const { delay = 0, times = defaultOffset(valueKeyframesAsList), type = defaultTransition.type || "keyframes", repeat, repeatType, repeatDelay = 0, ...remainingTransition } = valueTransition;
      let { ease = defaultTransition.ease || "easeOut", duration } = valueTransition;
      const calculatedDelay = typeof delay === "function" ? delay(elementIndex, numSubjects) : delay;
      const numKeyframes = valueKeyframesAsList.length;
      const createGenerator = isGenerator(type) ? type : generators == null ? void 0 : generators[type || "keyframes"];
      if (numKeyframes <= 2 && createGenerator) {
        let absoluteDelta = 100;
        if (numKeyframes === 2 && isNumberKeyframesArray(valueKeyframesAsList)) {
          const delta = valueKeyframesAsList[1] - valueKeyframesAsList[0];
          absoluteDelta = Math.abs(delta);
        }
        const springTransition = {
          ...defaultTransition,
          ...remainingTransition
        };
        if (duration !== void 0) {
          springTransition.duration = secondsToMilliseconds(duration);
        }
        const springEasing = createGeneratorEasing(springTransition, absoluteDelta, createGenerator);
        ease = springEasing.ease;
        duration = springEasing.duration;
      }
      duration ?? (duration = defaultDuration);
      const startTime = currentTime + calculatedDelay;
      if (times.length === 1 && times[0] === 0) {
        times[1] = 1;
      }
      const remainder = times.length - valueKeyframesAsList.length;
      remainder > 0 && fillOffset(times, remainder);
      valueKeyframesAsList.length === 1 && valueKeyframesAsList.unshift(null);
      if (repeat) {
        duration = calculateRepeatDuration(duration, repeat);
        const originalKeyframes = [...valueKeyframesAsList];
        const originalTimes = [...times];
        ease = Array.isArray(ease) ? [...ease] : [ease];
        const originalEase = [...ease];
        for (let repeatIndex = 0; repeatIndex < repeat; repeatIndex++) {
          valueKeyframesAsList.push(...originalKeyframes);
          for (let keyframeIndex = 0; keyframeIndex < originalKeyframes.length; keyframeIndex++) {
            times.push(originalTimes[keyframeIndex] + (repeatIndex + 1));
            ease.push(keyframeIndex === 0 ? "linear" : getEasingForSegment(originalEase, keyframeIndex - 1));
          }
        }
        normalizeTimes(times, repeat);
      }
      const targetTime = startTime + duration;
      addKeyframes(valueSequence, valueKeyframesAsList, ease, times, startTime, targetTime);
      maxDuration = Math.max(calculatedDelay + duration, maxDuration);
      totalDuration = Math.max(targetTime, totalDuration);
    };
    if (isMotionValue(subject)) {
      const subjectSequence = getSubjectSequence(subject, sequences);
      resolveValueSequence(keyframes, transition, getValueSequence("default", subjectSequence));
    } else {
      const subjects = resolveSubjects(subject, keyframes, scope, elementCache);
      const numSubjects = subjects.length;
      for (let subjectIndex = 0; subjectIndex < numSubjects; subjectIndex++) {
        keyframes = keyframes;
        transition = transition;
        const thisSubject = subjects[subjectIndex];
        const subjectSequence = getSubjectSequence(thisSubject, sequences);
        for (const key in keyframes) {
          resolveValueSequence(keyframes[key], getValueTransition(transition, key), getValueSequence(key, subjectSequence), subjectIndex, numSubjects);
        }
      }
    }
    prevTime = currentTime;
    currentTime += maxDuration;
  }
  sequences.forEach((valueSequences, element) => {
    for (const key in valueSequences) {
      const valueSequence = valueSequences[key];
      valueSequence.sort(compareByTime);
      const keyframes = [];
      const valueOffset = [];
      const valueEasing = [];
      for (let i = 0; i < valueSequence.length; i++) {
        const { at, value, easing } = valueSequence[i];
        keyframes.push(value);
        valueOffset.push(progress(0, totalDuration, at));
        valueEasing.push(easing || "easeOut");
      }
      if (valueOffset[0] !== 0) {
        valueOffset.unshift(0);
        keyframes.unshift(keyframes[0]);
        valueEasing.unshift(defaultSegmentEasing);
      }
      if (valueOffset[valueOffset.length - 1] !== 1) {
        valueOffset.push(1);
        keyframes.push(null);
      }
      if (!animationDefinitions.has(element)) {
        animationDefinitions.set(element, {
          keyframes: {},
          transition: {}
        });
      }
      const definition = animationDefinitions.get(element);
      definition.keyframes[key] = keyframes;
      const { type: _type, ...remainingDefaultTransition } = defaultTransition;
      definition.transition[key] = {
        ...remainingDefaultTransition,
        duration: totalDuration,
        ease: valueEasing,
        times: valueOffset,
        ...sequenceTransition
      };
    }
  });
  return animationDefinitions;
}
function getSubjectSequence(subject, sequences) {
  !sequences.has(subject) && sequences.set(subject, {});
  return sequences.get(subject);
}
function getValueSequence(name, sequences) {
  if (!sequences[name])
    sequences[name] = [];
  return sequences[name];
}
function keyframesAsList(keyframes) {
  return Array.isArray(keyframes) ? keyframes : [keyframes];
}
function getValueTransition(transition, key) {
  return transition && transition[key] ? {
    ...transition,
    ...transition[key]
  } : { ...transition };
}
const isNumber = (keyframe) => typeof keyframe === "number";
const isNumberKeyframesArray = (keyframes) => keyframes.every(isNumber);
function createDOMVisualElement(element) {
  const options = {
    presenceContext: null,
    props: {},
    visualState: {
      renderState: {
        transform: {},
        transformOrigin: {},
        style: {},
        vars: {},
        attrs: {}
      },
      latestValues: {}
    }
  };
  const node = isSVGElement(element) && !isSVGSVGElement(element) ? new SVGVisualElement(options) : new HTMLVisualElement(options);
  node.mount(element);
  visualElementStore.set(element, node);
}
function createObjectVisualElement(subject) {
  const options = {
    presenceContext: null,
    props: {},
    visualState: {
      renderState: {
        output: {}
      },
      latestValues: {}
    }
  };
  const node = new ObjectVisualElement(options);
  node.mount(subject);
  visualElementStore.set(subject, node);
}
function isSingleValue(subject, keyframes) {
  return isMotionValue(subject) || typeof subject === "number" || typeof subject === "string" && !isDOMKeyframes(keyframes);
}
function animateSubject(subject, keyframes, options, scope) {
  const animations = [];
  if (isSingleValue(subject, keyframes)) {
    animations.push(animateSingleValue(subject, isDOMKeyframes(keyframes) ? keyframes.default || keyframes : keyframes, options ? options.default || options : options));
  } else {
    if (subject == null) {
      return animations;
    }
    const subjects = resolveSubjects(subject, keyframes, scope);
    const numSubjects = subjects.length;
    for (let i = 0; i < numSubjects; i++) {
      const thisSubject = subjects[i];
      const createVisualElement = thisSubject instanceof Element ? createDOMVisualElement : createObjectVisualElement;
      if (!visualElementStore.has(thisSubject)) {
        createVisualElement(thisSubject);
      }
      const visualElement = visualElementStore.get(thisSubject);
      const transition = { ...options };
      if ("delay" in transition && typeof transition.delay === "function") {
        transition.delay = transition.delay(i, numSubjects);
      }
      animations.push(...animateTarget(visualElement, { ...keyframes, transition }, {}));
    }
  }
  return animations;
}
function animateSequence(sequence, options, scope) {
  const animations = [];
  const processedSequence = sequence.map((segment) => {
    if (Array.isArray(segment) && typeof segment[0] === "function") {
      const callback = segment[0];
      const mv = motionValue(0);
      mv.on("change", callback);
      if (segment.length === 1) {
        return [mv, [0, 1]];
      } else if (segment.length === 2) {
        return [mv, [0, 1], segment[1]];
      } else {
        return [mv, segment[1], segment[2]];
      }
    }
    return segment;
  });
  const animationDefinitions = createAnimationsFromSequence(processedSequence, options, scope, { spring });
  animationDefinitions.forEach(({ keyframes, transition }, subject) => {
    animations.push(...animateSubject(subject, keyframes, transition));
  });
  return animations;
}
function isSequence(value) {
  return Array.isArray(value) && value.some(Array.isArray);
}
function createScopedAnimate(options = {}) {
  const { scope, reduceMotion } = options;
  function scopedAnimate(subjectOrSequence, optionsOrKeyframes, options2) {
    let animations = [];
    let animationOnComplete;
    if (isSequence(subjectOrSequence)) {
      const { onComplete, ...sequenceOptions } = optionsOrKeyframes || {};
      if (typeof onComplete === "function") {
        animationOnComplete = onComplete;
      }
      animations = animateSequence(subjectOrSequence, reduceMotion !== void 0 ? { reduceMotion, ...sequenceOptions } : sequenceOptions, scope);
    } else {
      const { onComplete, ...rest } = options2 || {};
      if (typeof onComplete === "function") {
        animationOnComplete = onComplete;
      }
      animations = animateSubject(subjectOrSequence, optionsOrKeyframes, reduceMotion !== void 0 ? { reduceMotion, ...rest } : rest, scope);
    }
    const animation = new GroupAnimationWithThen(animations);
    if (animationOnComplete) {
      animation.finished.then(animationOnComplete);
    }
    if (scope) {
      scope.animations.push(animation);
      animation.finished.then(() => {
        removeItem(scope.animations, animation);
      });
    }
    return animation;
  }
  return scopedAnimate;
}
const animate = createScopedAnimate();
function FeaturedCarousel({ properties, title }) {
  const scrollRef = reactExports.useRef(null);
  if (properties.length === 0) return null;
  function scroll(dir) {
    if (!scrollRef.current) return;
    scrollRef.current.scrollBy({
      left: dir === "left" ? -280 : 280,
      behavior: "smooth"
    });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx("section", { className: "py-6", "data-ocid": "featured-carousel-section", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "max-w-6xl mx-auto px-4", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between mb-4", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "font-display text-xl sm:text-2xl font-semibold text-foreground", children: title }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-2", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            type: "button",
            onClick: () => scroll("left"),
            className: "w-8 h-8 rounded-full bg-card border border-border flex items-center justify-center hover:bg-primary hover:text-primary-foreground hover:border-primary transition-smooth shadow-card",
            "aria-label": "Scroll left",
            "data-ocid": "carousel-scroll-left",
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(ChevronLeft, { className: "w-4 h-4" })
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            type: "button",
            onClick: () => scroll("right"),
            className: "w-8 h-8 rounded-full bg-card border border-border flex items-center justify-center hover:bg-primary hover:text-primary-foreground hover:border-primary transition-smooth shadow-card",
            "aria-label": "Scroll right",
            "data-ocid": "carousel-scroll-right",
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(ChevronRight, { className: "w-4 h-4" })
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        ref: scrollRef,
        className: "flex gap-4 overflow-x-auto pb-2 snap-x snap-mandatory scroll-smooth",
        style: { scrollbarWidth: "none" },
        children: properties.map((property, i) => {
          const img = getPrimaryImageUrl(
            property.images,
            property.primaryImageIndex
          );
          return /* @__PURE__ */ jsxRuntimeExports.jsx(
            motion.div,
            {
              initial: { opacity: 0, x: 40 },
              animate: { opacity: 1, x: 0 },
              transition: { delay: i * 0.1, duration: 0.45 },
              className: "snap-start shrink-0 w-[240px] sm:w-[280px]",
              "data-ocid": `featured-card.item.${i + 1}`,
              children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                Link,
                {
                  to: "/property/$id",
                  params: { id: property.propertyId },
                  className: "block group",
                  children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-card rounded-xl overflow-hidden shadow-card border border-border/50 hover:shadow-hover transition-smooth", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "relative aspect-[4/3] overflow-hidden", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx(
                        "img",
                        {
                          src: img,
                          alt: property.title,
                          className: "w-full h-full object-cover group-hover:scale-105 transition-smooth duration-500",
                          loading: "lazy"
                        }
                      ),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent" }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute top-2 left-2", children: /* @__PURE__ */ jsxRuntimeExports.jsx(PropertyStatusBadge, { status: property.status }) }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute top-2 right-2", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "bg-primary text-primary-foreground text-xs font-bold px-2 py-1 rounded-lg", children: formatPrice(property.price) }) }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute bottom-2 left-3 right-3", children: /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-white text-sm font-display font-semibold line-clamp-1 drop-shadow", children: property.title }) })
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "p-3", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground text-xs line-clamp-1", children: property.locationName }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-foreground text-xs font-medium mt-1", children: formatArea(property.areaSizeSqFt) })
                    ] })
                  ] })
                }
              )
            },
            property.propertyId
          );
        })
      }
    )
  ] }) });
}
function AnimatedCounter({
  value,
  prefix = "",
  suffix = "",
  duration = 1.5,
  className
}) {
  const containerRef = reactExports.useRef(null);
  const motionVal = useMotionValue(0);
  const [display, setDisplay] = reactExports.useState(0);
  const [hasStarted, setHasStarted] = reactExports.useState(false);
  const rounded = useTransform(motionVal, (v) => Math.round(v));
  reactExports.useEffect(() => {
    const unsubscribe = rounded.on("change", (v) => setDisplay(v));
    return unsubscribe;
  }, [rounded]);
  reactExports.useEffect(() => {
    const el = containerRef.current;
    if (!el) return;
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting && !hasStarted) {
          setHasStarted(true);
          animate(motionVal, value, {
            duration,
            ease: "easeOut"
          });
        }
      },
      { threshold: 0.3 }
    );
    observer.observe(el);
    return () => observer.disconnect();
  }, [value, duration, hasStarted, motionVal]);
  reactExports.useEffect(() => {
    if (hasStarted) {
      animate(motionVal, value, {
        duration: duration * 0.6,
        ease: "easeOut"
      });
    }
  }, [value, hasStarted, duration, motionVal]);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    motion.span,
    {
      ref: containerRef,
      className: cn("tabular-nums", className),
      "data-ocid": "animated-counter",
      children: [
        prefix,
        display.toLocaleString("en-IN"),
        suffix
      ]
    }
  );
}
function AnimatedHeading({
  text,
  delay = 200
}) {
  const lines = text.split("\n");
  const charDelay = 30;
  const [started, setStarted] = reactExports.useState(false);
  reactExports.useEffect(() => {
    const t = setTimeout(() => setStarted(true), delay);
    return () => clearTimeout(t);
  }, [delay]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "h1",
    {
      className: "font-display text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-normal leading-tight mb-4",
      style: { letterSpacing: "-0.04em" },
      children: lines.map((line, lineIdx) => /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "block", children: line.split("").map((char, charIdx) => {
        const d = lineIdx * line.length * charDelay + charIdx * charDelay;
        return /* @__PURE__ */ jsxRuntimeExports.jsx(
          "span",
          {
            className: "inline-block transition-all duration-500",
            style: {
              opacity: started ? 1 : 0,
              transform: started ? "translateX(0)" : "translateX(-18px)",
              transitionDelay: `${d}ms`
            },
            children: char === " " ? " " : char
          },
          `${line}-${char.charCodeAt(0)}-${charIdx}`
        );
      }) }, line))
    }
  );
}
function FadeIn({
  children,
  delay = 0,
  duration = 1e3
}) {
  const [visible, setVisible] = reactExports.useState(false);
  reactExports.useEffect(() => {
    const t = setTimeout(() => setVisible(true), delay);
    return () => clearTimeout(t);
  }, [delay]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      className: "transition-opacity",
      style: {
        opacity: visible ? 1 : 0,
        transitionDuration: `${duration}ms`
      },
      children
    }
  );
}
const VIDEO_URL = "https://d8j0ntlcm91z4.cloudfront.net/user_38xzZboKViGWJOttwIXH07lWA1P/hf_20260403_050628_c4e32401-fab4-4a27-b7a8-6e9291cd5959.mp4";
function HomeHero({ properties, isLoading, lang }) {
  const available = properties.filter(
    (p) => p.status === PropertyStatus.available
  ).length;
  const sold = properties.filter(
    (p) => p.status === PropertyStatus.sold
  ).length;
  const total = isLoading ? 0 : properties.length;
  const headingText = lang === "hi" ? "आपकी सपनों की संपत्ति\nयहीं आपका इंतज़ार कर रही है" : "Your Dream Property\nAwaits You Here";
  const subheadingText = lang === "hi" ? "भारत के प्रमुख स्थानों पर प्रीमियम प्लॉट और भूमि खोजें।" : "Discover premium plots and land in prime locations across India.";
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: "relative overflow-hidden",
      style: { minHeight: "100vh" },
      "data-ocid": "hero-section",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "video",
          {
            autoPlay: true,
            muted: true,
            loop: true,
            playsInline: true,
            className: "global-video-bg",
            tabIndex: -1,
            "aria-hidden": "true",
            children: /* @__PURE__ */ jsxRuntimeExports.jsx("source", { src: VIDEO_URL, type: "video/mp4" })
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "global-video-fallback", "aria-hidden": "true" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "relative z-10 max-w-5xl mx-auto px-6 md:px-12 lg:px-16 flex flex-col justify-end h-screen pb-12 lg:pb-16", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("nav", { className: "absolute top-0 left-0 right-0 pt-6 px-6 md:px-12 lg:px-16", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "liquid-glass rounded-xl px-4 py-2 flex items-center justify-between", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-2xl font-semibold tracking-tight text-white", children: "Tapovanam Realty" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hidden md:flex items-center gap-8", children: ["Properties", "About", "Contact"].map((link) => /* @__PURE__ */ jsxRuntimeExports.jsx(
              "a",
              {
                href: `#${link.toLowerCase()}`,
                className: "text-sm text-gray-300 hover:text-white transition-colors duration-200",
                children: link
              },
              link
            )) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-0 md:w-auto" })
          ] }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "lg:grid lg:grid-cols-2 lg:items-end gap-8", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(AnimatedHeading, { text: headingText, delay: 200 }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(FadeIn, { delay: 800, duration: 1e3, children: /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-base md:text-lg text-gray-300 mb-5 max-w-xl", children: subheadingText }) })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex items-end justify-start lg:justify-end mt-6 lg:mt-0", children: /* @__PURE__ */ jsxRuntimeExports.jsx(FadeIn, { delay: 1400, duration: 1e3, children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "liquid-glass border border-white/20 px-6 py-3 rounded-xl", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-lg md:text-xl lg:text-2xl font-light text-white", children: "Buy. Sell. Invest." }) }) }) })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(FadeIn, { delay: 1600, duration: 1e3, children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex flex-wrap justify-center gap-3 sm:gap-6 mt-8", children: [
            {
              label: lang === "hi" ? "कुल संपत्तियां" : "Total Plots",
              value: total,
              color: "text-white"
            },
            {
              label: lang === "hi" ? "उपलब्ध" : "Available",
              value: available,
              color: "text-green-300"
            },
            {
              label: lang === "hi" ? "बिका हुआ" : "Sold",
              value: sold,
              color: "text-amber-300"
            }
          ].map(({ label, value, color }) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "div",
            {
              className: "bg-black/30 backdrop-blur-md border border-white/20 rounded-2xl px-5 py-4 min-w-[90px] text-center shadow-lg",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "div",
                  {
                    className: `font-display text-3xl sm:text-4xl font-bold ${color}`,
                    children: /* @__PURE__ */ jsxRuntimeExports.jsx(AnimatedCounter, { value })
                  }
                ),
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-white/70 text-xs mt-1 tracking-wide", children: label })
              ]
            },
            label
          )) }) })
        ] })
      ]
    }
  );
}
const STORAGE_KEY = "tapovanam_favorites";
function loadFavorites() {
  try {
    const raw = localStorage.getItem(STORAGE_KEY);
    if (!raw) return [];
    const parsed = JSON.parse(raw);
    return Array.isArray(parsed) ? parsed : [];
  } catch {
    return [];
  }
}
function useFavorites() {
  const [favorites, setFavorites] = reactExports.useState(loadFavorites);
  reactExports.useEffect(() => {
    try {
      localStorage.setItem(STORAGE_KEY, JSON.stringify(favorites));
    } catch {
    }
  }, [favorites]);
  const isFavorite = reactExports.useCallback(
    (id) => favorites.includes(id),
    [favorites]
  );
  const toggleFavorite = reactExports.useCallback((id) => {
    setFavorites(
      (prev) => prev.includes(id) ? prev.filter((f) => f !== id) : [...prev, id]
    );
  }, []);
  const clearFavorites = reactExports.useCallback(() => setFavorites([]), []);
  return { favorites, toggleFavorite, isFavorite, clearFavorites };
}
function FavoriteButton({ propertyId, className }) {
  const { isFavorite, toggleFavorite } = useFavorites();
  const fav = isFavorite(propertyId);
  function handleClick(e) {
    e.preventDefault();
    e.stopPropagation();
    toggleFavorite(propertyId);
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    motion.button,
    {
      type: "button",
      onClick: handleClick,
      whileTap: { scale: 0.85 },
      className: cn(
        "w-9 h-9 rounded-full flex items-center justify-center",
        "glass-light dark:glass-dark shadow-md",
        "transition-smooth focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring",
        className
      ),
      "aria-label": fav ? "Remove from favorites" : "Add to favorites",
      "data-ocid": "favorite-button",
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(AnimatePresence, { mode: "wait", initial: false, children: fav ? /* @__PURE__ */ jsxRuntimeExports.jsx(
        motion.span,
        {
          initial: { scale: 0.5, opacity: 0 },
          animate: { scale: [1.4, 1], opacity: 1 },
          exit: { scale: 0.5, opacity: 0 },
          transition: { type: "spring", stiffness: 500, damping: 22 },
          children: /* @__PURE__ */ jsxRuntimeExports.jsx(Heart, { className: "w-4 h-4 fill-red-500 text-red-500" })
        },
        "filled"
      ) : /* @__PURE__ */ jsxRuntimeExports.jsx(
        motion.span,
        {
          initial: { scale: 0.5, opacity: 0 },
          animate: { scale: 1, opacity: 1 },
          exit: { scale: 0.5, opacity: 0 },
          transition: { duration: 0.15 },
          children: /* @__PURE__ */ jsxRuntimeExports.jsx(Heart, { className: "w-4 h-4 text-white drop-shadow" })
        },
        "outline"
      ) })
    }
  );
}
function ThreeDCard({
  children,
  className,
  intensity = 15,
  glare = true
}) {
  const cardRef = reactExports.useRef(null);
  const rawX = useMotionValue(0);
  const rawY = useMotionValue(0);
  const glareX = useMotionValue(50);
  const glareY = useMotionValue(50);
  const springConfig = { stiffness: 300, damping: 30 };
  const rotateX = useSpring(
    useTransform(rawY, [-0.5, 0.5], [intensity, -intensity]),
    springConfig
  );
  const rotateY = useSpring(
    useTransform(rawX, [-0.5, 0.5], [-intensity, intensity]),
    springConfig
  );
  const scale = useSpring(1, { stiffness: 300, damping: 25 });
  function handleMouseMove(e) {
    const el = cardRef.current;
    if (!el) return;
    const rect = el.getBoundingClientRect();
    const x = (e.clientX - rect.left) / rect.width - 0.5;
    const y = (e.clientY - rect.top) / rect.height - 0.5;
    rawX.set(x);
    rawY.set(y);
    glareX.set((e.clientX - rect.left) / rect.width * 100);
    glareY.set((e.clientY - rect.top) / rect.height * 100);
  }
  function handleMouseLeave() {
    rawX.set(0);
    rawY.set(0);
    scale.set(1);
  }
  function handleMouseEnter() {
    scale.set(1.02);
  }
  function handleTouchMove(e) {
    const el = cardRef.current;
    if (!el || !e.touches[0]) return;
    const rect = el.getBoundingClientRect();
    const x = (e.touches[0].clientX - rect.left) / rect.width - 0.5;
    const y = (e.touches[0].clientY - rect.top) / rect.height - 0.5;
    rawX.set(x * 0.5);
    rawY.set(y * 0.5);
  }
  function handleTouchEnd() {
    rawX.set(0);
    rawY.set(0);
  }
  const glareGradient = useTransform(
    [glareX, glareY],
    ([gx, gy]) => `radial-gradient(circle at ${gx}% ${gy}%, rgba(255,255,255,0.18) 0%, transparent 60%)`
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { ref: cardRef, className: cn("card-tilt", className), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
    motion.div,
    {
      style: { rotateX, rotateY, scale, transformStyle: "preserve-3d" },
      onMouseMove: handleMouseMove,
      onMouseLeave: handleMouseLeave,
      onMouseEnter: handleMouseEnter,
      onTouchMove: handleTouchMove,
      onTouchEnd: handleTouchEnd,
      className: "relative w-full h-full",
      children: [
        children,
        glare && /* @__PURE__ */ jsxRuntimeExports.jsx(
          motion.div,
          {
            className: "absolute inset-0 rounded-xl pointer-events-none z-10",
            style: { background: glareGradient }
          }
        )
      ]
    }
  ) });
}
function PropertyCard({
  property,
  index = 0,
  dealerWhatsApp
}) {
  var _a;
  const imageUrl = getPrimaryImageUrl(
    property.images,
    property.primaryImageIndex
  );
  const isAvailable = ((_a = property.status) == null ? void 0 : _a.toLowerCase()) === "available";
  const isNew = Date.now() - Number(property.dateAdded) / 1e6 < 7 * 24 * 60 * 60 * 1e3;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    motion.div,
    {
      initial: { opacity: 0, y: 24 },
      animate: { opacity: 1, y: 0 },
      transition: { delay: index * 0.07, duration: 0.45, ease: "easeOut" },
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(ThreeDCard, { intensity: 12, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        Link,
        {
          to: "/property/$id",
          params: { id: property.propertyId },
          className: "block group outline-none",
          "data-ocid": "property-card",
          children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "article",
            {
              className: [
                "bg-card rounded-2xl overflow-hidden shadow-xl transition-smooth",
                "focus-within:ring-2 focus-within:ring-ring",
                !isAvailable ? "opacity-80" : ""
              ].join(" "),
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "relative overflow-hidden h-[200px] w-full", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    "img",
                    {
                      src: imageUrl,
                      alt: property.title,
                      className: "w-full h-full object-cover transition-smooth duration-500 group-hover:scale-108",
                      loading: "lazy"
                    }
                  ),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute inset-0 bg-gradient-to-t from-black/65 via-transparent to-transparent" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute top-3 right-3", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "bg-primary text-primary-foreground text-sm font-bold px-3 py-1.5 rounded-xl shadow-gold-glow", children: formatPrice(property.price) }) }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "absolute top-3 left-3 flex flex-col gap-1.5", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(PropertyStatusBadge, { status: property.status }),
                    isNew && /* @__PURE__ */ jsxRuntimeExports.jsxs(Badge, { className: "text-[10px] font-bold uppercase tracking-wide bg-amber-400/90 text-amber-950 border-0 px-2 py-0.5 gap-1", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx(Sparkles, { className: "w-2.5 h-2.5" }),
                      "New"
                    ] })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute top-3 right-3 mt-10", children: /* @__PURE__ */ jsxRuntimeExports.jsx(FavoriteButton, { propertyId: property.propertyId }) }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute bottom-0 left-0 right-0 p-4", children: /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "font-display text-lg font-semibold text-white line-clamp-1 drop-shadow", children: property.title }) })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "p-4 space-y-3", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-start gap-1.5 text-muted-foreground", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(MapPin, { className: "w-4 h-4 shrink-0 mt-0.5 text-primary" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-sm line-clamp-1", children: property.locationName })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-1.5 text-muted-foreground", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Ruler, { className: "w-4 h-4 shrink-0 text-primary" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-sm font-medium", children: formatArea(property.areaSizeSqFt) })
                  ] }),
                  property.description && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm text-muted-foreground line-clamp-2 leading-relaxed", children: property.description }),
                  dealerWhatsApp && /* @__PURE__ */ jsxRuntimeExports.jsxs(
                    "button",
                    {
                      type: "button",
                      onClick: (e) => {
                        e.stopPropagation();
                        e.preventDefault();
                        const msg = encodeURIComponent(
                          `Hi, I am interested in ${property.title} at ${property.locationName}. Price: ${formatPrice(property.price)}, Area: ${formatArea(property.areaSizeSqFt)}. Please share more details.`
                        );
                        window.open(
                          `https://wa.me/${dealerWhatsApp}?text=${msg}`,
                          "_blank"
                        );
                      },
                      className: "w-full flex items-center justify-center gap-2 bg-amber-500 hover:bg-amber-600 text-white text-sm font-semibold py-2.5 rounded-lg transition-colors",
                      "data-ocid": "property-whatsapp-button",
                      children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx(MessageCircle, { className: "w-4 h-4" }),
                        "WhatsApp"
                      ]
                    }
                  )
                ] })
              ]
            }
          )
        }
      ) })
    }
  );
}
function PropertyCardSkeleton() {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-card rounded-xl overflow-hidden shadow-card", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "aspect-[4/3] w-full" }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "p-4 space-y-3", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-4 w-3/4" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-4 w-1/2" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-3 w-full" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-3 w-5/6" })
    ] })
  ] });
}
const SORT_OPTIONS = [
  { value: "newest", label: "Newest First", labelHi: "नवीनतम पहले" },
  { value: "price_asc", label: "Price: Low → High", labelHi: "मूल्य: कम → अधिक" },
  {
    value: "price_desc",
    label: "Price: High → Low",
    labelHi: "मूल्य: अधिक → कम"
  },
  {
    value: "area_desc",
    label: "Area: Large → Small",
    labelHi: "क्षेत्र: बड़ा → छोटा"
  }
];
function FilterPanel({
  location,
  minPrice,
  maxPrice,
  minArea,
  maxArea,
  status,
  sortBy,
  lang,
  onLocationChange,
  onMinPriceChange,
  onMaxPriceChange,
  onMinAreaChange,
  onMaxAreaChange,
  onStatusChange,
  onSortChange,
  onReset,
  activeFilterCount
}) {
  const t = (en, hi) => lang === "hi" ? hi : en;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    motion.div,
    {
      initial: { opacity: 0, y: -8 },
      animate: { opacity: 1, y: 0 },
      exit: { opacity: 0, y: -8 },
      transition: { duration: 0.25 },
      className: "bg-card border border-border rounded-xl p-5 shadow-card space-y-5",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "font-display text-base font-semibold text-foreground", children: t("Filters", "फ़िल्टर") }),
          activeFilterCount > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "button",
            {
              type: "button",
              onClick: onReset,
              className: "flex items-center gap-1 text-xs text-primary hover:text-primary/80 transition-colors",
              "data-ocid": "filter-reset",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "w-3 h-3" }),
                t("Clear all", "सब साफ करें")
              ]
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(Label, { className: "text-xs font-medium uppercase tracking-wide text-muted-foreground flex items-center gap-1", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(ArrowDownUp, { className: "w-3 h-3" }),
            t("Sort By", "क्रमबद्ध करें")
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex flex-col gap-1.5", "data-ocid": "filter-sort", children: SORT_OPTIONS.map((opt) => /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              type: "button",
              onClick: () => onSortChange(opt.value),
              className: [
                "px-3 py-1.5 rounded-lg text-xs font-medium text-left border transition-smooth",
                sortBy === opt.value ? "bg-primary text-primary-foreground border-primary" : "bg-background text-foreground border-border hover:border-primary/50"
              ].join(" "),
              children: lang === "hi" ? opt.labelHi : opt.label
            },
            opt.value
          )) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs font-medium uppercase tracking-wide text-muted-foreground", children: t("Status", "स्थिति") }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex gap-2 flex-wrap", "data-ocid": "filter-status", children: ["available", "all", "sold"].map((s) => /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              type: "button",
              onClick: () => onStatusChange(s),
              className: [
                "px-3 py-1.5 rounded-lg text-xs font-medium capitalize border transition-smooth",
                status === s ? "bg-primary text-primary-foreground border-primary" : "bg-background text-foreground border-border hover:border-primary/50"
              ].join(" "),
              children: s === "all" ? t("All", "सभी") : s === "available" ? t("Available", "उपलब्ध") : t("Sold", "बिका")
            },
            s
          )) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Label,
            {
              htmlFor: "filter-location",
              className: "text-xs font-medium uppercase tracking-wide text-muted-foreground",
              children: t("Location", "स्थान")
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "relative", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(MapPin, { className: "absolute left-3 top-1/2 -translate-y-1/2 w-3.5 h-3.5 text-muted-foreground" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Input,
              {
                id: "filter-location",
                placeholder: t("City, area...", "शहर, क्षेत्र..."),
                value: location,
                onChange: (e) => onLocationChange(e.target.value),
                className: "pl-8 text-sm h-9",
                "data-ocid": "filter-location-input"
              }
            )
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs font-medium uppercase tracking-wide text-muted-foreground", children: t("Price Range (₹)", "मूल्य सीमा (₹)") }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "grid grid-cols-2 gap-2", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Input,
              {
                type: "number",
                placeholder: t("Min", "न्यूनतम"),
                value: minPrice,
                onChange: (e) => onMinPriceChange(e.target.value),
                className: "text-sm h-9",
                "data-ocid": "filter-min-price"
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Input,
              {
                type: "number",
                placeholder: t("Max", "अधिकतम"),
                value: maxPrice,
                onChange: (e) => onMaxPriceChange(e.target.value),
                className: "text-sm h-9",
                "data-ocid": "filter-max-price"
              }
            )
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs font-medium uppercase tracking-wide text-muted-foreground", children: t("Area Size (sq ft)", "क्षेत्रफल (वर्ग फुट)") }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "grid grid-cols-2 gap-2", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Input,
              {
                type: "number",
                placeholder: t("Min", "न्यूनतम"),
                value: minArea,
                onChange: (e) => onMinAreaChange(e.target.value),
                className: "text-sm h-9",
                "data-ocid": "filter-min-area"
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Input,
              {
                type: "number",
                placeholder: t("Max", "अधिकतम"),
                value: maxArea,
                onChange: (e) => onMaxAreaChange(e.target.value),
                className: "text-sm h-9",
                "data-ocid": "filter-max-area"
              }
            )
          ] })
        ] })
      ]
    }
  );
}
function EmptyState({
  hasFilters,
  onReset,
  lang
}) {
  const t = (en, hi) => lang === "hi" ? hi : en;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    motion.div,
    {
      initial: { opacity: 0, scale: 0.95 },
      animate: { opacity: 1, scale: 1 },
      transition: { duration: 0.4 },
      className: "flex flex-col items-center justify-center py-20 px-6 text-center",
      "data-ocid": "property-grid.empty_state",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          motion.div,
          {
            animate: { y: [0, -8, 0] },
            transition: {
              duration: 3,
              repeat: Number.POSITIVE_INFINITY,
              ease: "easeInOut"
            },
            className: "w-20 h-20 rounded-2xl bg-muted flex items-center justify-center mb-5 shadow-card",
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(House, { className: "w-10 h-10 text-muted-foreground" })
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "font-display text-xl font-semibold text-foreground mb-2", children: hasFilters ? t("No properties match your filters", "कोई संपत्ति नहीं मिली") : t("No properties yet", "अभी कोई संपत्ति नहीं") }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm text-muted-foreground max-w-xs leading-relaxed mb-6", children: hasFilters ? t(
          "Try adjusting your search or filters to find the perfect plot.",
          "अपनी खोज या फ़िल्टर बदलें।"
        ) : t(
          "Properties will appear here once the agent adds listings.",
          "एजेंट द्वारा संपत्तियां जोड़े जाने पर यहाँ दिखेंगी।"
        ) }),
        hasFilters && /* @__PURE__ */ jsxRuntimeExports.jsxs(
          Button,
          {
            variant: "outline",
            size: "sm",
            onClick: onReset,
            "data-ocid": "empty-state-clear-filters",
            className: "gap-2",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "w-3.5 h-3.5" }),
              t("Clear Filters", "फ़िल्टर साफ करें")
            ]
          }
        )
      ]
    }
  );
}
function useDebounce(value, delay) {
  const [debounced, setDebounced] = reactExports.useState(value);
  reactExports.useEffect(() => {
    const timer = setTimeout(() => setDebounced(value), delay);
    return () => clearTimeout(timer);
  }, [value, delay]);
  return debounced;
}
function RecentlyAddedSection({
  properties,
  lang
}) {
  if (properties.length === 0) return null;
  const t = (en, hi) => lang === "hi" ? hi : en;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    FeaturedCarousel,
    {
      properties,
      title: t("Recently Added", "हाल ही में जोड़ी गई")
    }
  );
}
function HomePage() {
  const [urlParams, setUrlParams] = reactExports.useState(
    () => new URLSearchParams(window.location.search)
  );
  const queryClient = useQueryClient();
  const [lang, setLang] = reactExports.useState("en");
  const [sortBy, setSortBy] = reactExports.useState("newest");
  reactExports.useEffect(() => {
    const handlePop = () => setUrlParams(new URLSearchParams(window.location.search));
    window.addEventListener("popstate", handlePop);
    return () => window.removeEventListener("popstate", handlePop);
  }, []);
  function updateParams(updates) {
    const next = new URLSearchParams(urlParams.toString());
    for (const [key, value] of Object.entries(updates)) {
      if (value) {
        next.set(key, value);
      } else {
        next.delete(key);
      }
    }
    const newUrl = `${window.location.pathname}?${next.toString()}`;
    window.history.replaceState(null, "", newUrl);
    setUrlParams(next);
  }
  const [searchInput, setSearchInput] = reactExports.useState(
    urlParams.get("location") ?? ""
  );
  const [showFilters, setShowFilters] = reactExports.useState(false);
  const [isRefreshing, setIsRefreshing] = reactExports.useState(false);
  const [showConstructionOnly, setShowConstructionOnly] = reactExports.useState(false);
  const location = urlParams.get("location") ?? "";
  const minPrice = urlParams.get("minPrice") ?? "";
  const maxPrice = urlParams.get("maxPrice") ?? "";
  const minArea = urlParams.get("minArea") ?? "";
  const maxArea = urlParams.get("maxArea") ?? "";
  const status = urlParams.get("status") ?? "available";
  const debouncedSearch = useDebounce(searchInput, 300);
  const isFirstMount = reactExports.useRef(true);
  reactExports.useEffect(() => {
    if (isFirstMount.current) {
      isFirstMount.current = false;
      return;
    }
    const next = new URLSearchParams(window.location.search);
    if (debouncedSearch) {
      next.set("location", debouncedSearch);
    } else {
      next.delete("location");
    }
    const newUrl = `${window.location.pathname}?${next.toString()}`;
    window.history.replaceState(null, "", newUrl);
    setUrlParams(next);
  }, [debouncedSearch]);
  const handleReset = reactExports.useCallback(() => {
    setSearchInput("");
    setSortBy("newest");
    window.history.replaceState(null, "", window.location.pathname);
    setUrlParams(new URLSearchParams());
  }, []);
  const handleRefresh = reactExports.useCallback(async () => {
    setIsRefreshing(true);
    await queryClient.invalidateQueries({ queryKey: PROPERTIES_QUERY_KEY });
    setTimeout(() => setIsRefreshing(false), 600);
  }, [queryClient]);
  const { data: properties, isLoading } = useProperties();
  const { data: dealerProfile } = useDealerProfile();
  const filtered = reactExports.useMemo(() => {
    if (!properties) return [];
    const base = properties.filter((p) => {
      if (status === "available" && p.status !== PropertyStatus.available)
        return false;
      if (status === "sold" && p.status !== PropertyStatus.sold) return false;
      const q = location.toLowerCase().trim();
      if (q && !p.title.toLowerCase().includes(q) && !p.locationName.toLowerCase().includes(q))
        return false;
      if (minPrice && Number(p.price) < Number(minPrice)) return false;
      if (maxPrice && Number(p.price) > Number(maxPrice)) return false;
      if (minArea && p.areaSizeSqFt < Number(minArea)) return false;
      if (maxArea && p.areaSizeSqFt > Number(maxArea)) return false;
      if (showConstructionOnly && !p.isConstructionSite) return false;
      return true;
    });
    return [...base].sort((a, b) => {
      if (sortBy === "price_asc") return Number(a.price) - Number(b.price);
      if (sortBy === "price_desc") return Number(b.price) - Number(a.price);
      if (sortBy === "area_desc") return b.areaSizeSqFt - a.areaSizeSqFt;
      return Number(b.dateAdded) - Number(a.dateAdded);
    });
  }, [
    properties,
    status,
    location,
    minPrice,
    maxPrice,
    minArea,
    maxArea,
    sortBy,
    showConstructionOnly
  ]);
  const featuredProperties = reactExports.useMemo(() => {
    if (!properties || properties.length === 0) return [];
    return [...properties].sort((a, b) => Number(b.dateAdded) - Number(a.dateAdded)).slice(0, 4);
  }, [properties]);
  const recentProperties = reactExports.useMemo(() => {
    if (!properties || properties.length <= 4) return [];
    return [...properties].sort((a, b) => Number(b.dateAdded) - Number(a.dateAdded)).slice(4, 7);
  }, [properties]);
  const activeFilterCount = [
    location,
    minPrice,
    maxPrice,
    minArea,
    maxArea,
    status !== "available" ? status : "",
    sortBy !== "newest" ? sortBy : ""
  ].filter(Boolean).length;
  const hasFilters = activeFilterCount > 0;
  const t = (en, hi) => lang === "hi" ? hi : en;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "min-h-screen bg-background", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      HomeHero,
      {
        properties: properties ?? [],
        isLoading,
        lang
      }
    ),
    !isLoading && featuredProperties.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "bg-muted/30 border-y border-border/50", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
      FeaturedCarousel,
      {
        properties: featuredProperties,
        title: t("Featured Properties", "विशेष संपत्तियां")
      }
    ) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "sticky top-0 z-20 bg-card/95 backdrop-blur-sm border-b border-border shadow-card", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "max-w-6xl mx-auto px-4 py-3", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "relative flex-1", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Search, { className: "absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-gray-400" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Input,
          {
            type: "search",
            placeholder: t(
              "Search locations, plots...",
              "स्थान, प्लॉट खोजें..."
            ),
            value: searchInput,
            onChange: (e) => setSearchInput(e.target.value),
            className: "pl-9 pr-9 h-10 bg-background text-foreground placeholder:text-gray-400 border-input focus:border-primary text-sm",
            "data-ocid": "home-search-input"
          }
        ),
        searchInput && /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            type: "button",
            onClick: () => setSearchInput(""),
            className: "absolute right-3 top-1/2 -translate-y-1/2 text-muted-foreground hover:text-foreground transition-colors",
            "aria-label": "Clear search",
            "data-ocid": "search-clear",
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "w-3.5 h-3.5" })
          }
        )
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(
        Button,
        {
          variant: "outline",
          size: "sm",
          onClick: () => setShowFilters((v) => !v),
          className: [
            "shrink-0 gap-1.5 h-10 px-3",
            showFilters ? "bg-primary text-primary-foreground border-primary hover:bg-primary/90" : ""
          ].join(" "),
          "data-ocid": "filter-toggle",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(SlidersHorizontal, { className: "w-4 h-4" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hidden sm:inline text-sm", children: t("Filters", "फ़िल्टर") }),
            activeFilterCount > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { className: "ml-0.5 h-4 w-4 p-0 flex items-center justify-center text-[10px] bg-primary-foreground text-primary", children: activeFilterCount }),
            showFilters ? /* @__PURE__ */ jsxRuntimeExports.jsx(ChevronUp, { className: "w-3.5 h-3.5 hidden sm:block" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(ChevronDown, { className: "w-3.5 h-3.5 hidden sm:block" })
          ]
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          type: "button",
          onClick: () => setLang((l) => l === "en" ? "hi" : "en"),
          className: "shrink-0 h-10 px-3 rounded-lg border border-border bg-card text-xs font-semibold text-foreground hover:bg-muted transition-smooth",
          "aria-label": "Toggle language",
          "data-ocid": "lang-toggle",
          children: lang === "en" ? "EN" : "HI"
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          type: "button",
          onClick: () => setShowConstructionOnly((v) => !v),
          className: [
            "shrink-0 h-10 px-3 rounded-lg border text-xs font-semibold transition-smooth",
            showConstructionOnly ? "bg-amber-500 text-white border-amber-500" : "bg-card text-foreground border-border hover:border-amber-400 hover:text-amber-700"
          ].join(" "),
          "aria-label": "Toggle construction sites",
          "data-ocid": "construction-toggle",
          children: "🏗"
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        Button,
        {
          variant: "ghost",
          size: "icon",
          onClick: handleRefresh,
          "aria-label": "Refresh listings",
          className: "shrink-0 h-10 w-10",
          "data-ocid": "refresh-btn",
          children: /* @__PURE__ */ jsxRuntimeExports.jsx(
            RefreshCw,
            {
              className: ["w-4 h-4", isRefreshing ? "animate-spin" : ""].join(
                " "
              )
            }
          )
        }
      )
    ] }) }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "max-w-6xl mx-auto px-4 py-6", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "lg:grid lg:grid-cols-[280px_1fr] lg:gap-6 lg:items-start", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "div",
        {
          className: [
            "lg:sticky lg:top-20",
            showFilters ? "block mb-5" : "hidden lg:block"
          ].join(" "),
          children: /* @__PURE__ */ jsxRuntimeExports.jsx(AnimatePresence, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(
            FilterPanel,
            {
              location,
              minPrice,
              maxPrice,
              minArea,
              maxArea,
              status,
              sortBy,
              lang,
              onLocationChange: (v) => updateParams({ location: v || void 0 }),
              onMinPriceChange: (v) => updateParams({ minPrice: v || void 0 }),
              onMaxPriceChange: (v) => updateParams({ maxPrice: v || void 0 }),
              onMinAreaChange: (v) => updateParams({ minArea: v || void 0 }),
              onMaxAreaChange: (v) => updateParams({ maxArea: v || void 0 }),
              onStatusChange: (v) => updateParams({ status: v !== "available" ? v : void 0 }),
              onSortChange: setSortBy,
              onReset: handleReset,
              activeFilterCount
            }
          ) })
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        !isLoading && recentProperties.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "mb-6 -mx-4 sm:mx-0", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          RecentlyAddedSection,
          {
            properties: recentProperties,
            lang
          }
        ) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-baseline justify-between mb-5", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "font-display text-2xl sm:text-3xl font-semibold text-amber-700 dark:text-amber-400", children: showConstructionOnly ? t("Construction Sites", "निर्माण स्थल") : t("Browse Properties", "संपत्तियां देखें") }),
          !isLoading && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-sm text-muted-foreground shrink-0 ml-3", children: [
            filtered.length,
            " ",
            filtered.length === 1 ? t("listing", "संपत्ति") : t("listings", "संपत्तियां")
          ] })
        ] }),
        isLoading && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-4", children: Array.from({ length: 6 }, (_, i) => `skeleton-${i}`).map(
          (id) => /* @__PURE__ */ jsxRuntimeExports.jsx(PropertyCardSkeleton, {}, id)
        ) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(AnimatePresence, { children: !isLoading && filtered.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsx(
          EmptyState,
          {
            hasFilters,
            onReset: handleReset,
            lang
          }
        ) }),
        !isLoading && filtered.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx(
          motion.div,
          {
            className: "grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-4",
            "data-ocid": "property-grid",
            initial: "hidden",
            animate: "visible",
            variants: {
              hidden: {},
              visible: { transition: { staggerChildren: 0.05 } }
            },
            children: filtered.map((property, idx) => /* @__PURE__ */ jsxRuntimeExports.jsx(
              motion.div,
              {
                variants: {
                  hidden: { opacity: 0, y: 20, scale: 0.97 },
                  visible: { opacity: 1, y: 0, scale: 1 }
                },
                transition: { duration: 0.35, ease: "easeOut" },
                "data-ocid": `property-grid.item.${idx + 1}`,
                children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                  PropertyCard,
                  {
                    property,
                    dealerWhatsApp: (dealerProfile == null ? void 0 : dealerProfile.whatsappNumber) || ""
                  }
                )
              },
              property.propertyId
            ))
          }
        ),
        !isLoading && recentProperties.length === 0 && featuredProperties.length === 0 && properties && properties.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mt-8 pt-6 border-t border-border/50 flex items-center gap-2 text-xs text-muted-foreground", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Clock, { className: "w-3.5 h-3.5" }),
          t("All listings shown above", "सभी संपत्तियां ऊपर दिखाई गई हैं")
        ] })
      ] })
    ] }) })
  ] });
}
export {
  HomePage
};
